import express from 'express';


const router = express.Router();

// router.post('/login',validateRequest(AuthValidation.loginZodSchema), AuthController.getSingleAdmin);


export const AuthRoutes = router;
